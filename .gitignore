__pycache__/
*.pyc
*.pyo
.DS_Store
*.log
.venv/
.claude/

# Personal data — gitignored so others get a clean slate on clone
resell_inventory.xlsx
config.yaml
user-preferences.yaml

# Issue logs (generated per-session, not tracked)
logs/issues/*.json

# Photo staging (user's photos, not tracked)
photo-inbox/
!photo-inbox/.gitkeep
