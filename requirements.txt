# resell-bot Python dependencies
# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Inventory spreadsheet management (resell-skill/scripts/update_inventory.py)
openpyxl>=3.1.0

# HEIC photo conversion (resell-skill/scripts/convert_heic.py)
Pillow>=10.0.0
pillow-heif>=0.13.0

# Configuration file parsing (config.yaml and user-preferences.yaml)
pyyaml>=6.0
