Check for the user's Telegram replies and act on pending decisions.

Find the workspace:
  find /sessions -name "resell_inventory.xlsx" 2>/dev/null | head -1 | xargs dirname

Read [WORKSPACE]/CLAUDE.md to understand the project, its purpose, and all behavioral rules before doing anything else.

Then read [WORKSPACE]/skills/scheduled-runs/followup-run.md and follow every step.
Chrome browser tools are available.
